//import all files in directory

public class Main {
    public static void main(String[] args) {
        ECC ecc = new ECC();
        RSA rsa = new RSA();
        TDES tdes = new TDES();
        AES aes = new AES();
        DES des = new DES();
        File file = new File();
    }
    // take the file name as input
    // read the file
    // encrypt the file with all the algorithms
    // decrypt the file with all the algorithms
    // create report on speed, reliability, and security

}
